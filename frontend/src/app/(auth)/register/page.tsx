import { RegisterForm } from '@/features/auth/components/RegisterForm';

export const metadata = {
  title: 'Register - Riba-X',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
